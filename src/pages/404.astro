---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { Button } from "@/components/ui/button";
---

<BaseLayout title="Página no encontrada">
  <section class="min-h-[80vh] flex items-center justify-center bg-background pt-24">
    <div class="max-w-2xl mx-auto px-6 text-center">
      <span class="text-accent uppercase tracking-[0.2em] text-xs font-bold mb-4 block">Error 404</span>
      <h1 class="text-6xl md:text-8xl font-serif mb-8">Oops</h1>
      <p class="text-xl text-muted-foreground font-light leading-relaxed mb-12">
        Parece que esta propiedad ya no está disponible. Pero no se preocupe, tenemos muchas otras opciones esperándole.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/">
          <Button className="btn-premium" client:load>
            Volver al Inicio
          </Button>
        </a>
        <a href="/propiedades">
          <Button variant="outline" className="border-primary text-primary hover:bg-primary hover:text-white uppercase tracking-widest text-xs font-bold px-8 py-4" client:load>
            Ver Propiedades
          </Button>
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
