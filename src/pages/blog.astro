---
import BaseLayout from "@/layouts/BaseLayout.astro";
---

<BaseLayout title="Römenn Journal">
  <main class="flex-grow section-padding container mx-auto px-6 py-32">
    <h1 class="text-4xl md:text-5xl font-serif mb-12 text-center">Römenn Journal</h1>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {[1, 2, 3].map((i) => (
        <article class="group cursor-pointer">
          <div class="aspect-video bg-muted mb-4 overflow-hidden">
            <div class="w-full h-full bg-gray-200 transition-transform duration-700 group-hover:scale-105"></div>
          </div>
          <span class="text-xs font-sans text-accent font-bold uppercase tracking-widest">Tendencias</span>
          <h3 class="text-xl font-serif mt-2 mb-2 group-hover:text-primary transition-colors">El futuro del mercado inmobiliario en Rivas</h3>
          <p class="text-muted-foreground font-sans text-sm line-clamp-3">Análisis profundo sobre la evolución de precios y demanda en el sector premium...</p>
        </article>
      ))}
    </div>
  </main>
</BaseLayout>
